databases:
  - name: uptime-db
    plan: free

services:
  - type: web
    name: uptime-monitor-web
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: uptime-db
          property: connectionString
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: CHECKER_SECRET_TOKEN
        generateValue: true

  - type: cron
    name: uptime-checker-cron
    plan: free
    schedule: "*/5 * * * *"
    buildCommand: "curl -X POST ${WEB_SERVICE_URL}/run-check/${CHECKER_SECRET_TOKEN}"
    envVars:
      - key: WEB_SERVICE_URL
        value: "https://uptime-monitor-web.onrender.com"
      - key: CHECKER_SECRET_TOKEN
        fromService:
          type: web
          name: uptime-monitor-web
          envVarKey: CHECKER_SECRET_TOKEN